using Unity.Entities;
using UnityEngine;

/// <summary>
/// Represents a component that makes an entity attach to its caster as a child in hierarchy.
/// </summary>
public struct AttachToCaster : IComponentData { }
